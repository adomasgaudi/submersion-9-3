<template lang="pug">
h1 outdated
h4 However, the language learning industry is inflexible enough to support this growing trend as that would undermine the significance of thousands of grammar books. 
h4 Actually, your school is likely even worse off, since it is probably forced by the local education departments to follow a grammar book even if the teacher had other ideas.
</template>

<script lang="ts">
// import {fire, fireAuth} from '@/firebase/config'
// import { useStore } from "vuex";
// import { onMounted, ref, provide } from "vue";

export default {
  // components: [],
  setup() {
    // const ss = useStore().state;
    // const sg = useStore().getters;
    ///////////////////////

    ///////////css////////////
    // css import
    // onMounted(() => {});
    ////////////////////////
    return {
      // ss, sg
    };
  }
};
</script>

<style lang="scss">
// #template {
// }
</style>
